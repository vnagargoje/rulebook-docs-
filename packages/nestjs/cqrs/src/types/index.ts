export interface DeepvueConfig {
    clientId: string
    clientSecret: string
    apiKey: string
    baseUrl: string
}

export interface MoovingConfig {
    companyId: string
    secretKey: string
    baseUrl: string
}

export interface RazorpayConfig {
    apiKey: string
    apiSecret: string
    webhookSecret: string
}
