export enum BatteryStatus {
    AVAILABLE = 'available',
    CHARGED = 'charged',
    CHARGING = 'charging',
    DRAINED = 'drained',
    IN_TRANSIT = 'in_transit',
    IN_USE = 'in_use',
}

export enum BatteryMovementStatus {
    IN_TRANSIT = 'in_transit',
    DELIVERED = 'delivered',
}
